// 23/09
// 1. Peça ao usuário um vetor de n elementos inteiros. Depois, duplique o tamanho incluíndo os mesmos elementos com sinal negativo. Imprima o vetor ordenado.
realloc(P, sizeof(int)* tam*2); //tam é tamanho
for
P(tam+i) = -P[i];


// 2. Dado um arquivo de texto que contém uma sequência de números inteiros, com comprimento desconhecido, faça uma função que armazena esses numeros em um vetor cujo tamanho seja exatamente o da sequencia de numeros. 
// IMPORTANTE: Leia o arquivo uma só vez!
tam = 0;
while (fscanf("%d", &x));
  p = realloc(p, sizeof(int)*(++tam));
  p[tam-1] = x;



// judge ex1 - fatorial

void fat(int n) {
  i *= i-1;


int main() {
  int min, max;
  int count;
  scanf("%d %d", &min &max);
  for (int i = 0; i < max; i++) {
    
  }
  printf("%d\n", count);
  return 0;
}
    
